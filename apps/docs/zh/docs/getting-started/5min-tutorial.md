# 五分钟完成一个任务

从小而可复核的仓库任务开始：

```sh
volund chat "阅读失败的测试，做最小修复，运行对应测试，然后展示 diff。"
```

Volund 会读取上下文、提出需要授权的操作、流式展示模型输出，并在本地记录会话。检查每一个权限弹窗和最终 diff，提交前仍应亲自运行项目测试。

发布 dog-food 证据必须使用真实 Anthropic provider，覆盖读取、编辑、测试和创建 PR，并把不含凭据或敏感内容的决策与链接记录到 `docs/releases/L1-DOGFOOD.md`。
